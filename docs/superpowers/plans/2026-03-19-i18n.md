# Demo 頁 i18n 多語言 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 `demo/App.vue` 新增繁體中文 / English 語言切換功能，透過自製 `useLocale()` composable 和 `LangSwitcher.vue` 元件實作，零外部依賴。

**Architecture:** 建立 `demo/i18n/` 資料夾存放字典與 composable；`LangSwitcher.vue` 為獨立 UI 元件；App.vue 整合兩者，用 `t('key')` 取代所有硬編碼中文字串。module-level `locale` ref 確保全 app 共享語言狀態。

**Tech Stack:** Vue 3 Composition API, TypeScript, Vitest, jsdom

---

## 檔案異動總覽

| 動作 | 路徑 | 說明 |
|---|---|---|
| 新增 | `demo/i18n/zh-TW.ts` | 繁體中文字典 |
| 新增 | `demo/i18n/en.ts` | 英文字典 |
| 新增 | `demo/i18n/index.ts` | useLocale() composable |
| 新增 | `demo/components/LangSwitcher.vue` | Globe 圖示 + 下拉選單 |
| 修改 | `demo/App.vue` | 整合 useLocale()，t('key') 取代硬編碼文字 |
| 新增 | `tests/demo/useLocale.test.ts` | useLocale() 單元測試 |
| 新增 | `tests/demo/LangSwitcher.test.ts` | LangSwitcher.vue 元件測試 |

---

## Task 1: 建立字典檔案

**Files:**
- Create: `demo/i18n/zh-TW.ts`
- Create: `demo/i18n/en.ts`

> 不需要測試（靜態資料物件）。

- [ ] **Step 1: 建立 `demo/i18n/zh-TW.ts`**

```ts
const zhTW = {
  stopMic:          '⏹ 停止麥克風',
  startMic:         '🎤 開啟麥克風',
  settings:         '⚙ 設定',
  code:             '</> 程式碼',
  status:           '狀態',
  mode:             '模式',
  recording:        '錄音中',
  idle:             '待機',
  micError:         '無法存取麥克風：',
  silenceDetected:  '偵測到靜音',
  generalSettings:  '通用設定',
  modeSettings:     '模式設定',
  height:           '高度 (height)',
  color:            '波形顏色 (color)',
  bgColor:          '背景顏色 (backgroundColor)',
  silenceThreshold: '靜音閾值 (silenceThreshold)',
  silenceDuration:  '靜音時長 (silenceDuration) ms',
  barCount:         'Bar 數量 (barCount)',
  lineWidth:        '線條寬度 (lineWidth)',
  fftSize:          'FFT 大小 (fftSize)',
  smoothing:        '平滑係數 (smoothingTimeConstant)',
  reset:            '重設',
  save:             '儲存',
  unsavedDot:       '有未儲存的變更',
  closePanel:       '關閉（放棄變更）',
  quickStart:       '快速使用指南',
  installPkg:       '安裝套件',
  pasteComponent:   '貼入你的元件',
  codeTipLine1:     '依照以下步驟將 WaveVisualizer 嵌入你的 Vue 3 專案。',
  codeTipLine2:     '程式碼已反映目前儲存的設定（模式：{mode}）。',
  copied:           '✓ 已複製',
  copy:             '複製',
  micBtnInCode:     '開啟麥克風',
  stopBtnInCode:    '停止',
} as const

export default zhTW
```

- [ ] **Step 2: 建立 `demo/i18n/en.ts`**

```ts
const en = {
  stopMic:          '⏹ Stop Mic',
  startMic:         '🎤 Start Mic',
  settings:         '⚙ Settings',
  code:             '</> Code',
  status:           'Status',
  mode:             'Mode',
  recording:        'Recording',
  idle:             'Idle',
  micError:         'Cannot access microphone: ',
  silenceDetected:  'Silence detected',
  generalSettings:  'General',
  modeSettings:     'Mode Settings',
  height:           'Height',
  color:            'Color',
  bgColor:          'Background Color',
  silenceThreshold: 'Silence Threshold',
  silenceDuration:  'Silence Duration ms',
  barCount:         'Bar Count',
  lineWidth:        'Line Width',
  fftSize:          'FFT Size',
  smoothing:        'Smoothing',
  reset:            'Reset',
  save:             'Save',
  unsavedDot:       'Unsaved changes',
  closePanel:       'Close (discard changes)',
  quickStart:       'Quick Start',
  installPkg:       'Install',
  pasteComponent:   'Add to your component',
  codeTipLine1:     'Follow these steps to embed WaveVisualizer in your Vue 3 project.',
  codeTipLine2:     'Code reflects your current saved settings (mode: {mode}).',
  copied:           '✓ Copied',
  copy:             'Copy',
  micBtnInCode:     'Start Mic',
  stopBtnInCode:    'Stop',
} as const

export default en
```

- [ ] **Step 3: Commit**

```bash
git add demo/i18n/zh-TW.ts demo/i18n/en.ts
git commit -m "feat: add zh-TW and en i18n dictionaries"
```

---

## Task 2: 建立 useLocale() composable

**Files:**
- Create: `demo/i18n/index.ts`
- Create: `tests/demo/useLocale.test.ts`

- [ ] **Step 1: 建立測試檔案 `tests/demo/useLocale.test.ts`**

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { useLocale } from '../../demo/i18n'

describe('useLocale', () => {
  beforeEach(() => {
    // 每次測試前重設為預設語言，避免 module-level ref 狀態污染
    const { setLocale } = useLocale()
    setLocale('zh-TW')
  })

  it('預設語言為 zh-TW', () => {
    const { locale } = useLocale()
    expect(locale.value).toBe('zh-TW')
  })

  it('t() 在 zh-TW 回傳中文字串', () => {
    const { t } = useLocale()
    expect(t('startMic')).toBe('🎤 開啟麥克風')
    expect(t('save')).toBe('儲存')
  })

  it('setLocale("en") 切換為英文', () => {
    const { setLocale, locale } = useLocale()
    setLocale('en')
    expect(locale.value).toBe('en')
  })

  it('t() 在 en 回傳英文字串', () => {
    const { setLocale, t } = useLocale()
    setLocale('en')
    expect(t('startMic')).toBe('🎤 Start Mic')
    expect(t('save')).toBe('Save')
  })

  it('切換語言後 t() 即時反映新語言', () => {
    const { setLocale, t } = useLocale()
    expect(t('reset')).toBe('重設')
    setLocale('en')
    expect(t('reset')).toBe('Reset')
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
npm test -- --reporter=verbose tests/demo/useLocale.test.ts
```

預期：`FAIL`，錯誤為 `Cannot find module '../../demo/i18n'`

- [ ] **Step 3: 建立 `demo/i18n/index.ts`**

```ts
import { ref } from 'vue'
import zhTW from './zh-TW'
import en from './en'

export type Locale = 'zh-TW' | 'en'
export type Messages = typeof zhTW

// module-level ref：全 app 共享同一個語言狀態
const locale = ref<Locale>('zh-TW')
const messages: Record<Locale, Messages> = { 'zh-TW': zhTW, en }

export function useLocale() {
  const t = (key: keyof Messages): string => messages[locale.value][key]
  const setLocale = (lang: Locale) => { locale.value = lang }
  return { locale, t, setLocale }
}
```

- [ ] **Step 4: 執行測試確認通過**

```bash
npm test -- --reporter=verbose tests/demo/useLocale.test.ts
```

預期：`5 tests passed`

- [ ] **Step 5: Commit**

```bash
git add demo/i18n/index.ts tests/demo/useLocale.test.ts
git commit -m "feat: add useLocale composable with zh-TW/en support"
```

---

## Task 3: 建立 LangSwitcher.vue

**Files:**
- Create: `demo/components/LangSwitcher.vue`
- Create: `tests/demo/LangSwitcher.test.ts`

- [ ] **Step 1: 建立測試檔案 `tests/demo/LangSwitcher.test.ts`**

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import LangSwitcher from '../../demo/components/LangSwitcher.vue'
import { useLocale } from '../../demo/i18n'

describe('LangSwitcher', () => {
  beforeEach(() => {
    const { setLocale } = useLocale()
    setLocale('zh-TW')
  })

  it('預設不顯示下拉選單', () => {
    const wrapper = mount(LangSwitcher)
    expect(wrapper.find('[data-testid="lang-dropdown"]').exists()).toBe(false)
  })

  it('點擊 globe 按鈕後顯示下拉選單', async () => {
    const wrapper = mount(LangSwitcher)
    await wrapper.find('[data-testid="lang-btn"]').trigger('click')
    expect(wrapper.find('[data-testid="lang-dropdown"]').exists()).toBe(true)
  })

  it('顯示兩個語言選項', async () => {
    const wrapper = mount(LangSwitcher)
    await wrapper.find('[data-testid="lang-btn"]').trigger('click')
    const items = wrapper.findAll('[data-testid="lang-item"]')
    expect(items).toHaveLength(2)
  })

  it('點擊 English 後切換語言並關閉選單', async () => {
    const wrapper = mount(LangSwitcher)
    await wrapper.find('[data-testid="lang-btn"]').trigger('click')
    const items = wrapper.findAll('[data-testid="lang-item"]')
    await items[1].trigger('click') // English
    const { locale } = useLocale()
    expect(locale.value).toBe('en')
    expect(wrapper.find('[data-testid="lang-dropdown"]').exists()).toBe(false)
  })

  it('按 Escape 關閉選單', async () => {
    const wrapper = mount(LangSwitcher, { attachTo: document.body })
    await wrapper.find('[data-testid="lang-btn"]').trigger('click')
    expect(wrapper.find('[data-testid="lang-dropdown"]').exists()).toBe(true)
    await wrapper.trigger('keydown', { key: 'Escape' })
    expect(wrapper.find('[data-testid="lang-dropdown"]').exists()).toBe(false)
    wrapper.unmount()
  })
})
```

- [ ] **Step 2: 執行測試確認失敗**

```bash
npm test -- --reporter=verbose tests/demo/LangSwitcher.test.ts
```

預期：`FAIL`，錯誤為 `Cannot find module '../../demo/components/LangSwitcher.vue'`

- [ ] **Step 3: 建立 `demo/components/LangSwitcher.vue`**

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { useLocale, type Locale } from '../i18n'

const { locale, setLocale } = useLocale()
const open = ref(false)

const langs: { code: Locale; label: string }[] = [
  { code: 'zh-TW', label: '繁體中文' },
  { code: 'en',    label: 'English'  },
]

function toggle() { open.value = !open.value }

function select(lang: Locale) {
  setLocale(lang)
  open.value = false
}

function onKeydown(e: KeyboardEvent) {
  if (e.key === 'Escape') open.value = false
}

function onClickOutside(e: MouseEvent) {
  if (!(e.target as Element).closest('.lang-switcher')) {
    open.value = false
  }
}

onMounted(() => {
  document.addEventListener('click', onClickOutside)
  document.addEventListener('keydown', onKeydown)
})

onUnmounted(() => {
  document.removeEventListener('click', onClickOutside)
  document.removeEventListener('keydown', onKeydown)
})
</script>

<template>
  <div class="lang-switcher">
    <button
      class="lang-btn"
      data-testid="lang-btn"
      aria-label="選擇語言 / Select language"
      @click.stop="toggle"
    >
      <!-- Globe SVG icon -->
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none"
           stroke="currentColor" stroke-width="2"
           stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="10"/>
        <line x1="2" y1="12" x2="22" y2="12"/>
        <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
      </svg>
    </button>

    <div v-if="open" class="lang-dropdown" data-testid="lang-dropdown">
      <button
        v-for="lang in langs"
        :key="lang.code"
        class="lang-item"
        :class="{ 'lang-item--active': locale === lang.code }"
        data-testid="lang-item"
        @click.stop="select(lang.code)"
      >{{ lang.label }}</button>
    </div>
  </div>
</template>

<style scoped>
.lang-switcher {
  position: relative;
  display: inline-flex;
  align-items: center;
}

.lang-btn {
  background: none;
  border: none;
  color: #8b949e;
  cursor: pointer;
  padding: 4px 6px;
  border-radius: 6px;
  display: flex;
  align-items: center;
  transition: color 0.15s;
  line-height: 1;
}
.lang-btn:hover { color: #e6edf3; }

.lang-dropdown {
  position: absolute;
  top: calc(100% + 6px);
  left: 0;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, .5);
  min-width: 130px;
  z-index: 200;
  overflow: hidden;
}

.lang-item {
  display: block;
  width: 100%;
  padding: 9px 16px;
  font-size: 0.82rem;
  color: #8b949e;
  background: none;
  border: none;
  border-bottom: 1px solid #30363d;
  cursor: pointer;
  text-align: left;
  transition: background 0.1s, color 0.1s;
}
.lang-item:last-child { border-bottom: none; }
.lang-item:hover { background: #21262d; color: #e6edf3; }
.lang-item--active { color: #58a6ff; background: #1f3a5f; }
</style>
```

- [ ] **Step 4: 執行測試確認通過**

```bash
npm test -- --reporter=verbose tests/demo/LangSwitcher.test.ts
```

預期：`5 tests passed`

- [ ] **Step 5: Commit**

```bash
git add demo/components/LangSwitcher.vue tests/demo/LangSwitcher.test.ts
git commit -m "feat: add LangSwitcher component with globe icon and dropdown"
```

---

## Task 4: 整合至 App.vue

**Files:**
- Modify: `demo/App.vue`

> 這個 Task 為整合，主要以手動驗證為主（切換語言、確認所有文字正確切換）。

- [ ] **Step 1: 在 `<script setup>` 頂部引入 useLocale 和 LangSwitcher**

在 `demo/App.vue` 的 import 區塊加入：

```ts
import { useLocale } from './i18n'
import LangSwitcher from './components/LangSwitcher.vue'

const { t } = useLocale()
```

- [ ] **Step 2: 替換 h1 列，加入 LangSwitcher**

找到：
```html
<h1>vue-wave-visualizer demo</h1>
```

取代為：
```html
<div class="title-row">
  <h1>vue-wave-visualizer demo</h1>
  <LangSwitcher />
</div>
```

並在 `<style>` 區塊加入：
```css
.title-row {
  display: flex;
  align-items: center;
  gap: 10px;
}
```

- [ ] **Step 3: 替換所有硬編碼中文字串**

依照下表逐一替換 template 中的文字：

| 原本 | 取代為 |
|---|---|
| `'⏹ 停止麥克風'` | `t('stopMic')` |
| `'🎤 開啟麥克風'` | `t('startMic')` |
| `'⚙ 設定'` | `t('settings')` |
| `'</> 程式碼'` | `t('code')` |
| `'狀態：'` | `t('status') + '：'` |
| `'模式：'` | `t('mode') + '：'` |
| `'錄音中'` | `t('recording')` |
| `'待機'` | `t('idle')` |
| `'設定'`（panel header h2） | `t('settings')` |
| `'有未儲存的變更'`（title） | `t('unsavedDot')` |
| `'關閉（放棄變更）'`（title） | `t('closePanel')` |
| `'通用設定'` | `t('generalSettings')` |
| `'高度 (height)'` | `t('height')` |
| `'波形顏色 (color)'` | `t('color')` |
| `'背景顏色 (backgroundColor)'` | `t('bgColor')` |
| `'靜音閾值 (silenceThreshold)'`（含 span） | `t('silenceThreshold')` |
| `'靜音時長 (silenceDuration) ms'` | `t('silenceDuration')` |
| `'模式設定'` | `t('modeSettings')` |
| `'Bar 數量 (barCount)'` | `t('barCount')` |
| `'線條寬度 (lineWidth)'` | `t('lineWidth')` |
| `'FFT 大小 (fftSize)'` | `t('fftSize')` |
| `'平滑係數 (smoothingTimeConstant)'`（含 span） | `t('smoothing')` |
| `'重設'` | `t('reset')` |
| `'儲存'` | `t('save')` |
| `'快速使用指南'` | `t('quickStart')` |
| `'安裝套件'` | `t('installPkg')` |
| `'貼入你的元件'` | `t('pasteComponent')` |
| `'複製'` / `'✓ 已複製'` | `t('copy')` / `t('copied')` |

- [ ] **Step 4: 替換動態字串**

**micError**（在 `catch` 區塊）：
```ts
// 原本
micError.value = '無法存取麥克風：' + (e instanceof Error ? e.message : String(e))
// 改為
micError.value = t('micError') + (e instanceof Error ? e.message : String(e))
```

**silenceMsg**（在 `onSilence`）：
```ts
// 原本
silenceMsg.value = `偵測到靜音 (${(payload.duration / 1000).toFixed(1)}s)`
// 改為
silenceMsg.value = `${t('silenceDetected')} (${(payload.duration / 1000).toFixed(1)}s)`
```

**codeTip**（在 template 的 `<p class="code-tip">`）：
```html
<!-- 原本 -->
<p class="code-tip">依照以下步驟將 WaveVisualizer 嵌入你的 Vue 3 專案。<br>程式碼已反映目前儲存的設定（模式：<strong>{{ mode }}</strong>）。</p>

<!-- 改為 -->
<p class="code-tip">
  {{ t('codeTipLine1') }}<br>
  {{ t('codeTipLine2').replace('{mode}', mode) }}
</p>
```

**usageCode 中的按鈕文字**（在 `usageCode` computed，使用 `t()` 需要注意 computed 的響應性）：

由於 `t()` 依賴 `locale` ref（響應式），`usageCode` 本身也是 `computed`，所以 `t()` 在 computed 內部可正確追蹤依賴，切換語言時會自動重算。

```ts
// 找到 usageCode computed 中的中文按鈕文字
`<button @click="startMic">開啟麥克風</button>`
// 改為
`<button @click="startMic">${t('micBtnInCode')}</button>`

`<button @click="stopMic">停止</button>`
// 改為
`<button @click="stopMic">${t('stopBtnInCode')}</button>`
```

- [ ] **Step 5: 確認無殘留硬編碼中文字串**

```bash
grep -n "停止麥克風\|開啟麥克風\|偵測到靜音\|無法存取\|通用設定\|模式設定\|波形顏色\|背景顏色\|靜音閾值\|靜音時長\|線條寬度\|平滑係數\|重設\b\|儲存\b\|快速使用\|安裝套件\|貼入你" demo/App.vue
```

預期：無任何輸出（表示所有硬編碼中文已替換完畢）

- [ ] **Step 6: 執行全部測試確認無回歸**

```bash
npm test
```

預期：所有測試通過（包含原有的 library 測試 + 新增的 demo 測試）

- [ ] **Step 7: 手動驗證**

```bash
npm run dev
```

開啟 `http://localhost:5173`，確認：
1. h1 右側出現 globe 圖示，與 h1 垂直置中
2. 點擊 globe → 下拉選單出現，顯示「繁體中文」（高亮藍色）和「English」
3. 選擇 English → 所有 UI 文字切換為英文
4. 選擇繁體中文 → 所有 UI 文字切回中文
5. 點擊選單外部 → 選單關閉
6. 按 Escape → 選單關閉
7. 開啟設定面板，確認所有 label 正確切換
8. 開啟程式碼 dialog，確認文字正確切換（包含 codeTip 和按鈕文字）

- [ ] **Step 8: Commit**

```bash
git add demo/App.vue
git commit -m "feat: integrate i18n into App.vue with zh-TW/en support"
```
